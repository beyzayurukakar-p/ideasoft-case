import { StyleSheet } from 'react-native';
import { dimensions } from '../../styling/dimensions';

export const headerRightSwitchStyles = StyleSheet.create({
  container: {
    paddingRight: dimensions.pageMargin,
  },
});
